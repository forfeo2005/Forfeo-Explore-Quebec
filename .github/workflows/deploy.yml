name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

# Permissions nécessaires pour le déploiement
permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: 'npm'

      - name: Install dependencies 🔧
        run: npm ci

      - name: Build 🏗️
        run: npm run build
        # Cela va créer le dossier 'dist' configuré dans vite.config.ts

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Le dossier que Vite génère
          branch: gh-pages # La branche où le site sera hébergé
